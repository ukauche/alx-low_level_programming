#include "main.h"
/**
 * _isupper - check for lower
 * @c: char to be checked
 * Return: 1 for uppercase & 0  for !=uppercase
 */
int _isupper(int c)
{

if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
